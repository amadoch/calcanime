/*
var carriage = 0;

function summatory () {
    let entry = promptEntry.textContent.toString();
    if (entry.match(/\d+( \+ )\d+/mg)) {
        let operation = entry.split(/( \+ )/);
        operation.forEach((member) => {
            _ = Number.parseFloat(member)
            if (Number.isInteger(_)) {
                carriage += _;
            }
        });
        promptEntry.textContent += ' + ';
    }
}
*/

// Tengo que hacer funciones matematicas AQUI